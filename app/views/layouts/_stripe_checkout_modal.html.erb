<!--


<form action="/billings/generate_payment" id="stripe_form" method="POST">
  <script src="https://checkout.stripe.com/checkout.js"
      class="stripe-button"
      data-key="<%= Rails.application.secrets.stripe_public_key %>"
      data-amount="2000"
      data-email="<%= current_user&.email %>"
      data-name="ComboSnacks"
      data-allow-remember-me="false"
      data-panel-label="Pagar {{amount}}"
      data-label="Confirmar Pedido"
      data-currency="CLP"
      data-image="/assets/logo-xs.png">
  </script>
</form>

 -->
<!--
<script src="https://checkout.stripe.com/checkout.js"></script>

<button id="customButton">Purchase</button>

<script>
var handler = StripeCheckout.configure({
  key: 'pk_test_gIe9xyRJTaUygZKdgR0GmaNj',
  image: 'https://stripe.com/img/documentation/checkout/marketplace.png',
  locale: 'auto',
  token: function(token) {
    // You can access the token ID with `token.id`.
    // Get the token ID to your server-side code for use.
  }
});

document.getElementById('customButton').addEventListener('click', function(e) {
  // Open Checkout with further options:
  handler.open({
    name: 'Property Simple',
    description: '2 widgets',
    amount: 2000
  });
  e.preventDefault();
});

// Close Checkout on page navigation:
window.addEventListener('popstate', function() {
  handler.close();
});
</script>
 -->
